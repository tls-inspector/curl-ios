# libcurl-ios

A simple build script to compile libcurl to a dylib framework for iOS apps.

# Instructions

It's as simple as:

```
./build-ios.sh <curl_version>
```

Then add the resulting curl.framework package to your app and you're done.